todo list:
s1.提取全局controller和network
s2.changeView
10.其他人副露手牌减少
11.摸牌效果
12.牌disabled状态

bug list:
2.手牌中已存在的杠不触发
3.吃牌state有误

blog list:
1.旋转后自身坐标系与children坐标系
2.es6 import export
3.Array.apply(null, { length: 136 }).map(function(value, index){
  return index;
})
4.抠文件script

improve list: 
1.减少计分板材料重制次数


# 2017/5/18
# when loss is incresing, one common problem is learning_rate is too high

# 2017/5/18
# higher batch_sizewill result in higher loss and higher gradient
# 2017/5/18
# l2_regularizer: lanmda range by training data size. set it like 1/10000